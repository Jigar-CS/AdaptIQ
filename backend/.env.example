<<<<<<< HEAD
PORT=5000

# Database (XAMPP defaults)
=======
# Environment Variables — copy to .env and fill in values
# All keys listed here are required unless marked (optional)

# Server
PORT=5000
NODE_ENV=development

# Database (XAMPP defaults shown)
>>>>>>> 1826be6 (Updated Phase 1 & 2)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=adaptiq

<<<<<<< HEAD
# JWT — use long random strings in production
JWT_SECRET=change_this_to_a_long_random_secret
JWT_REFRESH_SECRET=change_this_to_another_long_random_secret
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# CORS — frontend origin
CLIENT_ORIGIN=http://localhost:5173

# App
NODE_ENV=development
=======
# JWT
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=change_this_to_a_long_random_secret_min_32_chars
JWT_REFRESH_SECRET=change_this_to_another_long_random_secret_min_32_chars
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# File Uploads
# Path (relative to backend/) where uploaded files are stored
UPLOAD_DIR=uploads
# Max profile photo size in bytes (default 5MB)
MAX_PHOTO_SIZE=5242880
# Max resume size in bytes (default 10MB)
MAX_RESUME_SIZE=10485760

# Frontend origin (for CORS — change in production)
CLIENT_ORIGIN=http://localhost:5173
>>>>>>> 1826be6 (Updated Phase 1 & 2)
