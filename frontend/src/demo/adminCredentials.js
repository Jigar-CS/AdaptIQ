// Demo admin credentials for local development / demo purposes only.
// Matches the seeded admin user in `database/seed.sql`.

const demoAdmin = {
  email: 'admin@adaptiq.com',
  password: 'Admin@1234',
};

export default demoAdmin;
